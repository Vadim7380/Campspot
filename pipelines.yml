pool: 
  vmImage: 'windows-latest'

trigger:
  branches:
    include:
      - main
      - development

variables:
  solution: "**/*.sln"
  buildConfiguration: "Release"

stages:
  - stage: main
    dependsOn: []
    jobs:
      - job: jobOne
        displayName: "nameOne"
        steps:
          - script:
              dotnet restore
              dotnet build